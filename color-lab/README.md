# Color Lab

Interactive color palette generator with HSL sliders, contrast checker, color harmonies, and export.

## How to run

Open `index.html` in a browser.

## What you'd change

- Add palette saving/loading (localStorage)
- Add image color extraction (drop an image, get its palette)
- Add CSS gradient builder
