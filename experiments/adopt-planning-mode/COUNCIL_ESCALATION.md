# Council Escalation — experiments/adopt-planning-mode

**Gate:** plan
**Reason:** Unresolved objections after 2 attempts
**Timestamp:** 2026-04-09T19:56:58.732476+00:00

## Angle positions

### BUGS — OBJECT (high)
- **Reason:** The plan explicitly defers sanitization of potentially unsafe output (file paths, function names) generated by the LLM, creating a latent security vulnerability and correctness risk if the output is ever programmatically consumed, despite current assurances of human-only review.
- **Required fix:** Implement basic sanitization/validation for file paths and function names within the prompt's instructions to the agent, or add a pre-filter script to validate the `plan.md` output for basic safety and structural adherence before it's presented for human review. Relying solely on human review for correctness and deferring a known security risk is not robust.
- **Evidence:** `Trust-boundary note (added after SECURITY objection 2026-04-08): plan.md is explicitly a human-review artifact; no downstream automated parser consumes it. If that ever changes — i.e., if any script begins reading file paths or function names out of plan.md and acting on them — a sanitization layer `

### SECURITY — OBJECT (high)
- **Reason:** The modified prompt instructs an LLM to generate security-sensitive strings (file paths, function names) into a document without any sanitization or validation, creating a latent injection risk if the output is ever consumed by automated systems, despite current assurances.
- **Required fix:** The prompt must be updated to instruct the LLM to generate only safe, relative file paths and valid function names, or a sanitization/validation step must be introduced for the generated output before it is written to plan.md.
- **Evidence:** `The plan's 'Security' section states: 'No sanitization or validation is needed for the plan artifact itself because it is read by humans, not executed by scripts.' The 'Trust-boundary note' further clarifies: 'If that ever changes — i.e., if any script begins reading file paths or function names out`

### UI — APPROVE (low)
- **Reason:** The proposed changes provide clear, structured instructions for the AI agent, which will result in a more organized and verifiable plan artifact for human reviewers, improving readability and reducing cognitive load.

### GUIDE — APPROVE (low)
- **Reason:** The plan explicitly details how the prompt will be updated with imperative language and a clear, structured format, ensuring both AI agents and human reviewers can discover and understand the new planning mode requirements.
- **Evidence:** `Deliverable (experiments/adopt-planning-mode/plan.md) - ## Guide section, ## Approach section (What changes in tick_tock_prompt.md)`

### USEFULNESS — APPROVE (low)
- **Reason:** This project directly addresses the need for structured, verifiable AI-generated plans, making them more useful and efficient for human review.
- **Evidence:** `The current 'free-form prose' plans are a known pain point for human reviewers who need clear subtasks, file targets, and function changes to effectively approve work. This change enforces a specific, detailed format that directly solves this unmet need, similar to how a human architect would provid`

### COOL — APPROVE (low)
- **Reason:** The project introduces a signature planning methodology for the AI, enforcing a unique, detailed, and reviewable structure with explicit file and function mapping before code generation.

### LESSONS — APPROVE (low)
- **Reason:** No documented lessons are violated by the proposed plan. The plan proactively addresses potential issues raised in past insights.

## Resolution

Human decision required. Resume the build after updating session_state.json.
