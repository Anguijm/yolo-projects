# System Dashboard

Live system resource monitor: CPU, memory, disk, network, top processes. Auto-refreshes every 5s.

## How to run

```bash
python3 system_dash.py
```

Opens `http://localhost:8900`.
