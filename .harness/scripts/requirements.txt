# Gemini SDK for the council runner.
# Pinned to an exact version (Security council guard): PyPI version slots
# are immutable, so a pin to ==1.73.1 prevents auto-upgrade to a hijacked
# release. Upgrade by editing this line and re-pinning to a known SHA.
google-genai==1.73.1
