# Gemini SDK for the council runner.
#
# YOLO-PROJECTS DEVIATION: yolo's `.harness/scripts/council.py` (the
# bespoke gate-mode runner used by tick_tock.yml) imports
# `google.generativeai as genai` (the older SDK), NOT the new
# `google-genai` SDK that the canonical V0.3.1 template specifies.
# We install the SDK yolo actually imports rather than the canonical
# pin so any council invocation can `import google.generativeai`
# without ImportError. (Codex P1 caught this mismatch on PR #7.)
#
# The migration to `google-genai` is tracked alongside the
# diff-mode-council roadmap in `.github/workflows/council.yml`.
# When yolo's council.py migrates to the new SDK, this line should
# update to `google-genai==<pinned-version>` to match canonical.
#
# Pinned to an exact version (Security council guard): PyPI version slots
# are immutable, so a pin to ==<exact> prevents auto-upgrade to a
# hijacked release.
google-generativeai==0.8.3
