# Regex Playground

Live regex tester with match highlighting, group extraction, and a quick reference cheat sheet.

## How to run

Open `index.html` in a browser. That's it — pure client-side, no server needed.

## What you'd change

- Add replacement string testing
- Add regex flavor notes (JS vs Python differences)
- Save/share regex via URL hash
