# RESUME — Where to Start

**Last session ended:** 2026-04-25 evening JST. Loop paused by user.

This file is the canonical restart point. Read it first when picking the project back up.

---

## State at the time of pause

| Item | Value |
|---|---|
| Branch | `main`, clean, in sync with `origin/main` |
| Open council escalations | **1** (naval-scribe IMPL — UI drawer mutual-exclusion bug) |
| Deferred escalations | **1** (`experiments/adopt-bare-agent` PLAN — needs `CONSTRAINTS.md`) |
| In-flight tock | naval-scribe Letter Quality Checker + Portable Draft Export/Import (PLAN passed, IMPL has the open escalation above) |
| Tick queue head | `adopt-bare-agent` (deferred), then `model-eval-backbone`, then `strategic-niche-audit` |
| Phase 4 cron health | **success** (last run 2026-04-25 22:04 UTC, well within 12h freshness) |
| Phase 4 backlog | **19 items** awaiting cull on next daily run |
| Cron dispatch state | **HALTED** on the open escalation per standard rules — clear it before dispatching |

The cron will not auto-build while a council escalation is open. Resolve or defer the open escalation first.

---

## Read these files in this order on restart

1. **`_hot.md`** — live snapshot of portfolio + tick queue + recent builds. Auto-generated by `update_hot_cache.py`. ~33 lines.
2. **`.harness/session_state.json` -> `resume_instructions`** — terse one-paragraph pointer.
3. **`naval-scribe/COUNCIL_ESCALATION.md`** — the open escalation. Single concrete UI fix.
4. **`PHASE4_REPORT.md`** — full Phase 4 snapshot if you need the bigger picture.
5. **`.harness/scripts/council_rules.md`** — canonical reference for the 4 council enforcement rules (parse-retry, LESSONS precondition, goalpost-move, BUGS hallucination).

---

## Three natural restart paths (pick one)

### Path A (default — recommended): Ship the in-flight tock

The naval-scribe Letter Quality Checker is one bug fix away from shipping. The cron has already done PLAN + IMPL; only one UI objection blocks OUTCOME.

**The fix:**
- File: `naval-scribe/index.html`
- The `qualityBtn` click handler closes all other open drawers before opening its own.
- The `tmplBtn` and `routingBtn` click handlers do not. Council cited `naval-scribe/index.html:L1767` (tmplBtn) and `:L1968` (routingBtn).
- Required: copy the `qualityBtn` pattern (close-everything-else, then open) to both `tmplBtn` and `routingBtn` handlers.

**Steps:**
1. Read the `qualityBtn` handler to capture the close-everything-else pattern.
2. Apply the same pattern to `tmplBtn` and `routingBtn`.
3. `python3 test_project.py naval-scribe` — should still be 7/7.
4. Resolve the entry in `session_state.json -> council_escalations[]` (move to `council_escalations_resolved[]` with a resolution note).
5. Append a **Resolution** section to `naval-scribe/COUNCIL_ESCALATION.md`.
6. Commit (`tock: resolve naval-scribe Letter Quality Checker IMPL — drawer mutual-exclusion`).
7. Push.
8. Dispatch a fresh `Tick-Tock Builder (Hourly)` workflow run; expect TESTS + OUTCOME to pass and the tock to ship.

### Path B: Resolve the deferred infra tick

`experiments/adopt-bare-agent` PLAN is deferred because the council flagged the deliberately-permissive `run_shell(shell=True)` and `write_file` (path traversal) as critical SECURITY issues without an architectural acknowledgement.

**The fix:**
1. Create `experiments/adopt-bare-agent/CONSTRAINTS.md` declaring the trust model: "Local-developer-use only. `bare_agent.py` is a research-spike reference implementation, not a production agent. Permissive `subprocess.run(shell=True)` and unconstrained file paths are intentional minimalism — adding sandboxing would defeat the comparison-against-harness-cli purpose. Do not deploy to multi-tenant or untrusted environments." Match the `naval-scribe/CONSTRAINTS.md` pattern.
2. Move the entry from `council_escalations_deferred[]` back to `council_escalations[]` AND immediately to `council_escalations_resolved[]` with a resolution note pointing at the new CONSTRAINTS.md.
3. Append a Resolution section to `experiments/adopt-bare-agent/COUNCIL_ESCALATION.md`.
4. Commit + push + dispatch.

### Path C: Strategic queue reorder

The current queue has `adopt-bare-agent` (research spike) at the head. Two ticks deeper in the queue have higher signal-to-effort for loop quality:

- **`infra-failure-mode-audit`** (currently position 6) — mines the 63 resolved escalations to produce a failure-mode taxonomy. Could feed a 5th council enforcement rule.
- **`infra-loop-timing`** (currently position 7) — measures wall-clock time per loop stage to identify non-AI bottlenecks.

If you want to prioritize loop self-improvement over the research spike, move one or both ahead of `adopt-bare-agent` in `session_state.json -> tick_tock.tick_queue_approved[]`.

---

## Maintenance reminders

These were flagged in `STACK_AUDIT.md` and `PHASE4_REPORT.md` -> Risks. They are not session-blocking but become more urgent over time:

- **Node.js 20 EOL on 2026-04-30** — 4 days from snapshot. Cron runner installs `@anthropic-ai/claude-code` on Node 20. Fix is one line in `tick_tock.yml`. Queued as follow-up tick `infra-node-22-upgrade`.
- **`google-generativeai` SDK actively deprecated** — FutureWarning fires every cron run. ~3-month risk window. Migration target: `google-genai`. Queued as `infra-genai-migration`.
- **Unpinned pip packages** — supply-chain risk. Queued as `infra-pip-pinning`.

These are documented as Recommended Follow-Up Ticks in `experiments/adopt-stack-audit/STACK_AUDIT.md`. The user (John) gates approval — they have not yet been promoted into the tick queue.

---

## How this file stays current

`RESUME.md` is intentionally short and prescriptive. Update it whenever:
- The session ends with non-trivial state (open escalations, deferred items, in-flight tocks).
- The recommended next-action changes meaningfully.
- A new alternative path becomes relevant.

Do **not** treat it as a log. Old guidance gets replaced, not appended. The git history of this file is the audit trail of "what we thought to do next" over time.

---

*Generated 2026-04-25. Re-derive any number above by reading the cited source file.*
