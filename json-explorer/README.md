# JSON Explorer

Paste any JSON, explore it as a collapsible tree with search, path copying, and format/minify.

## How to run

Open `index.html` in a browser.

## What you'd change

- Add JSONPath query support
- Add file drag-and-drop
- Add diff mode (compare two JSON blobs)
