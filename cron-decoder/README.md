# Cron Decoder

Paste a cron expression, see a human-readable description, next 10 run times, and a weekly heatmap of when it fires.

## How to run

Open `index.html` in a browser.

## What you'd change

- Add 6-field cron (with seconds)
- Add timezone support
- Export to Google Calendar / iCal
